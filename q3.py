# Rodamos dijsktra para cada vértice na lista:
# O(L * (E + V)logV)
    # Pegando o maior valor de dijkstra para todos os vértices na lista
    # em relação a um vértice
    # O(V * L)
# Pegamos o menor valor dessa lista final
# O(V)